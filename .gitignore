# Local virtual environment
.venv/

# Model checkpoint (195 MB, downloaded by `ftw model download`)
*.ckpt

# Sentinel-2 inputs and FTW rasters (regenerated by the notebook)
ftw_input_*.tif
ftw_predictions*.tif

# Crop calendars and Hansen Global Forest Change granules
data/
[sw]c_[se]os_*.tif*

# Execution logs and the pre-run backup
nbrun*.log
*.ORIG.ipynb

# Python / Jupyter
__pycache__/
.ipynb_checkpoints/
